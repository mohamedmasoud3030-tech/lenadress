# خطة إطلاق LENA — سريعة • قوية • دقيقة
> فُحصت من **الكود الفعلي** (وليس الوثائق): كلون كامل + `npm ci` + build + typecheck + lint + **676 اختبارًا (كلها ناجحة)** + فحص أسرار + فحص اتصال Supabase حي.

**التاريخ:** 2026-08-07 — **الفرع:** `main` — **الحالة:** التطبيق جاهز فنياً للإطلاق بعد تنفيذ خطوات التشغيل أدناه.

---

## 1) موانع الإطلاق المكتشفة من الكود الفعلي

| # | المانع | الخطورة | الحالة |
|---|--------|---------|--------|
| 1 | **حساب المدير الأول غير قابل للإنشاء من داخل التطبيق** — صفحة الدخول فيها `signIn` فقط، وأول أدمن بيتعمل تلقائيًا بترغر `handle_new_auth_user` في `supabase/migrations/0009` عند إنشاء أول مستخدم في Supabase Auth. بدون إنشائه يدويًا: لا أحد يقدر يدخل التطبيق. | 🔴 حرجة | **خطوة يدوية في خطة الإطلاق** (القسم 3) |
| 2 | **التطبيق ويب = مرتبط بالإنترنت 100%** — `RequireAuth` + `CloudDataGate` بيقفلوا التطبيق كله بشاشة "تم إيقاف التشغيل لحماية البيانات" لو Supabase مش متاح، رغم أن التخزين محلي. الكود الفعلي بيكذّب وصف "local-first" في الوثائق. | 🔴 حرجة (مخاطرة تشغيل) | قرار معماري مقصود — يُدار بالمراقبة + إعادة المحاولة + نسخ احتياطية يومية. لا يُغير قبل الإطلاق |
| 3 | **`ReportsPage` كانت صفحة من تطبيق تاني** — هيدر مكتوب يدويًا (`text-3xl font-bold`) + 14 كارت مكرر يدوي بدون المكونات المشتركة. | 🟠 بصري | ✅ **أُصلحت** (تحت: القسم 2) |
| 4 | **`AppointmentsPage` هيدر يدوي مختلف** عن باقي الصفحات. | 🟠 بصري | ✅ **أُصلحت** |
| 5 | **الاندينج بلغة بصرية مختلفة عن التطبيق** — بنفسجي (violet) + هيرو متدرج داكن "قالب SaaS"، بينما التطبيق كهرماني/رمادي (amber/slate). | 🟠 بصري | ✅ **أُوحّدت** (تحت: القسم 2) |
| 6 | **زر أساسي بنفسجي في DressesPage** (`bg-violet-700`) بجانب الزر الأساسي الكهرماني/الداكن — زرّان أساسيان بلونين مختلفين. | 🟡 بسيطة | ✅ **أُصلح** |
| 7 | **`index.html` بدون meta description / Open Graph** — مشاركة اللاندينج على واتساب/إنستجرام هتطلع بدون وصف ولا صورة. | 🟡 تسويقي | ✅ **أُضيف** |
| 8 | **صورة الهيرو (jpg) مش precached في الـ PWA** — `globPatterns` كانت بدون jpg. | 🟡 بسيطة | ✅ **أُضيف** jpg/jpeg/webp |
| 9 | **متطلبات Node `>=22 <25`** — أي بيئة تشغيل/CI لازم Node 22 (CI شغالة على 22 ✓). | 🟡 بيئة | توثيق فقط (مطبّق فعلًا) |
| 10 | **ملفات قمامة في جذر الريبو**: `LENA_DRESS_ROOMSHOW_V020_BETA_COMPLETE.zip` (132KB)، `me`، `TEST_FILE.md` — ملهاش أي مرجع في الكود. | 🟢 نظافة | أوصى بالحذف: `git rm LENA_DRESS_ROOMSHOW_V020_BETA_COMPLETE.zip me TEST_FILE.md` |
| 11 | **`.env.production` متتبع في git** بمفتاح publishable حقيقي — آمن بفضل RLS (المفتاح للنشر العام أصلاً)، ولا يوجد أي `service_role` في الريبو ✓. | 🟢 أمان | مقبول — يُفضل نقل القيم لـ Vercel Environment Variables |
| 12 | **الاختبارات: 676/676 ناجحة** + build + typecheck + lint نظيف ✓ — لا يوجد مانع فني من الاختبارات. | ✅ | — |

---

## 2) الإصلاحات المنفذة (في هذه الجلسة — كلها متحققة بالكود)

1. **`src/features/reports/ReportsPage.tsx`** — إعادة بناء كاملة على المكونات المشتركة: `PageHeader` + `Section` + `SummaryCard` + `EmptyState` + حقول موحدة. نفس البيانات والمنطق بالضبط.
2. **`src/features/appointments/AppointmentsPage.tsx`** — `PageHeader` + `Section` + `EmptyState` + شارات حالة موحدة.
3. **`src/pages/landing/components/LandingHero.tsx`** — هيرو جديد كليًا: بوتيك دافئ (كهرماني/عاجي) بدل التدرج الداكن، مع صورة فستان أنيقة (`public/images/hero-dress.jpg`)، نفس الأزرار الوظيفية والإحصائيات.
4. **توحيد هوية اللاندينج** — كل الـ violet في `LandingHeader/ValueProps/Steps/Categories/AboutServices/Faq/Contact/Inventory` → amber (لغة التطبيق).
5. **توحيد التدرجات** — `AppShell` + `LoginPage` + `DressesPage` (gradients) → عائلة الكهرماني.
6. **`SummaryCard` tone accent** → sky بدل violet؛ **حالة "مؤجر"** → blue (متناسق مع شارات الدفعات).
7. **`DressesPage`** — الزر الأساسي الثاني أصبح ثانوي (outline) + شارات موحدة.
8. **`index.html`** — meta description + Open Graph + Twitter cards + العنوان "LENA | معرض فساتين المناسبات".
9. **`vite.config.ts`** — PWA precache يدعم الصور jpg/webp.

**التحقق:** `npm run build` ✓ • `npm run typecheck` ✓ • `eslint` ✓ • **`npm test` = 676/676 ✓** • المعاينة الحية شغالة ✓

---

## 3) خطة الإطلاق — 3 مراحل

### المرحلة أ: التجهيز (نصف يوم — مالك المشروع)

- [ ] **إنشاء حساب المدير الأول** (المانع #1):
  1. ادخل [Supabase Dashboard](https://supabase.com/dashboard) → مشروع `ktmizdznbdwvalmmfvfc`.
  2. **Authentication → Users → Add user** → بريد/باسورد لمديرة المعرض (اسم المستخدم بيتاخد من البريد تلقائيًا، أو من `full_name` في metadata).
  3. أول مستخدم بينعمل → الترغر بيعمله **admin** تلقائيًا (مؤكد من `migrations/0009`).
  4. جرّب تسجيل الدخول على `https://<الموقع>/login`.
- [ ] **التأكد من تطبيق الـ migrations الـ 18** — SQL Editor: `select version from supabase_migrations.schema_migrations order by version;` → لازم يطابق `supabase/migrations/` حتى `0018`.
- [ ] **النسخ الاحتياطي**: Project Settings → Database → Daily backups مفعّل + Backup Retention على الأقل 7 أيام.
- [ ] **المراقبة**: اضبط تنبيه (alert) على استهلاك Supabase (لو quota قربت تخلص) — البريد بيوصل من Supabase تلقائيًا.

### المرحلة ب: النشر (ساعة واحدة)

- [ ] **Vercel**: استيراد الريبو → Framework: Vite → Build: `npm run build` → Output: `dist` (موجودة في `vercel.json` ✓).
- [ ] **Environment Variables في Vercel** (نسخ من `.env.production`):
      `VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY`
- [ ] **Node 22**: Vercel بيقرأ `engines` من `package.json` تلقائيًا ✓.
- [ ] **الديسبلوي** → اختبر `https://<project>.vercel.app`.
- [ ] **الاسم: ربط دومين** (مثل `lena.om` أو subdomain) → SSL تلقائي.
- [ ] **التأكد من الـ deep links**: افتح `https://<domain>/login` و`/inventory` — الـ rewrites في `vercel.json` بتغطيها ✓.
- [ ] **SEO**: سجّل الدومين في Google Search Console + أرسل sitemap (`/` فقط كصفحة عامة — باقي المسارات محمية بدخول).

### المرحلة ج: يوم الإطلاق (Runbook — 30 دقيقة)

- [ ] 9:00 — تحقق من الـ status: صفحة الهبوط (`/`) تفتح، الصور ظاهرة، أزرار واتساب بتفتح `wa.me` برسالة صحيحة.
- [ ] تسجيل دخول المديرة على موبايل (أندرويد) + على التابلت/اللابتوب.
- [ ] إعداد **PIN الجهاز** (من الإعدادات) — القفل بيتفعل بعد تسجيل الدخول.
- [ ] اختبار سريع على الموبايل: إضافة فستان بصورة ← حجز ← استلام دفعة ← تسليم/استرجاع ← تقرير اليوم.
- [ ] تثبيت PWA: مشاركة الموقع → "إضافة للشاشة الرئيسية" (أيقونة LENA ✓).
- [ ] **إعلان الإطلاق**: مشاركة رابط اللاندينج على واتساب/إنستجرام — دلوقتي بينزل ببطاقة مشاركة (OG) احترافية ✓.
- [ ] إخبار الفريق: **التطبيق يحتاج إنترنت** — لو انقطع النت تظهر شاشة "إيقاف التشغيل لحماية البيانات" + زر إعادة المحاولة (سلوك مقصود، مش عطل).

### خطة التراجع (لو حصلت مشكلة بعد الإطلاق)
- **Rollback فوري**: في Vercel → Deployments → آخر ديسبلوي ناجح قبل الإطلاق → Redeploy (دقيقة واحدة).
- **البيانات**: كل عملية بتتعمل commit للـ cloud + snapshot محلي + نسخة إقفال يومية قابلة للتصدير — الخسارة القصوى = عمليات آخر دقائق قبل القطع.
- **طوارئ الحسابات**: `Authentication → Users` → إعادة تعيين باسورد لأي حساب.

---

## 4) ميثاق التصميم الموحد (يمنع رجوع "كل صفحة من تطبيق تاني")

1. **كل صفحة** تبدأ بـ `PageHeader` (eyebrow + title) — ممنوع كتابة `<h1>` يدويًا.
2. **كل قسم** داخل `Section` — ممنوع `rounded-2xl border ... bg-white p-5 shadow-sm` مكرر يدويًا.
3. **كل مؤشر** بـ `SummaryCard` (tones: default/positive/warning/danger/accent).
4. **حالات فارغة/تحميل/خطأ** بـ `EmptyState / LoadingState / ErrorState` فقط.
5. **الأزرار الأساسية**: `bg-slate-950` — الثانوية: outline أبيض. لون واحد أساسي فقط في الشاشة.
6. **اللغة البصرية**: كهرماني (amber) للتمييز + رمادي (slate/stone) للأسطح. البنفسجي = دلالي فقط (عربون/إكسسوار/خياطة). ممنوع إضافة hues جديدة بدون قرار.
7. **الاندينج** بنفس لغة التطبيق (هيرو كهرماني دافئ — مش تدرجات داكنة "قالب").
8. **فحص سريع قبل أي merge**: `grep -rn "violet\\|text-3xl font-bold" src/` لازم يرجّع صفر في الصفحات.

---

## 5) بعد الإطلاق (48 ساعة — حسب الأولوية)

- [ ] متابعة `client_error_events` في Supabase (التطبيق بيرسل أخطاء التشغيل تلقائيًا) + مراجعة سجل الأخطاء.
- [ ] حذف ملفات الجذر الميتة (المانع #10) — أوامر جاهزة في الجدول.
- [ ] نقل مفاتيح Supabase من `.env.production` (المتتبع في git) إلى Vercel Env فقط، وتحويل الملف لقالب.
- [ ] تدوير/إلغاء الـ PAT اللي اتحط في المحادثة دي بعد الانتهاء من المهام (GitHub → Settings → Developer settings → Personal access tokens).
- [ ] نسخة Tauri (ديسكتوب) — مسار منفصل يحتاج Rust toolchain؛ الويب/PWA هو الإطلاق الأساسي.
